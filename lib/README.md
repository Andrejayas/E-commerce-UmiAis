# Lib Directory

This directory contains utility functions, helpers, and shared logic for the application.

## Planned modules:
- `auth.ts` - Authentication helpers and password hashing
- `db.ts` - Database connection and ORM setup
- `validations.ts` - Zod schemas for form validation
- `utils.ts` - General utility functions
- `whatsapp.ts` - WhatsApp link generation
